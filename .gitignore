# exclude everything within the "codecs" and the "encodingOutput" folder but the folders themselves
# exclude everything within the "videoSources" folder but the .cfg files

# exclude everything
encodingOutput/*
videoSources/*
codecs/HM
codecs/VVCSoftware_VTM
codecs/vvenc

# exceptions to the rule
!encodingOutput/.gitkeep
!videoSources/.gitkeep
!videoSources/*.cfg
!codecs/.gitkeep